ALGORITHM read
VAR 
str : STRING[];
i,j,k: INTEGER;
begin
write("give me a sentence");
read(str);

int j(string str)
{
    int j = 0;
    for (int i=0; i<str.length(); i++)
        if (j(str[i])) 
            ++count;
    return j;

    str:= str.concat('.');

    write("my new sentence is"  : str);
    end 


